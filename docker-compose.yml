version: '3.9'

networks:
  rpg-internal:
    internal: true

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    networks:
      - rpg-internal
    environment:
      - NODE_ENV=production
      - CLIENT_ORIGIN=${CLIENT_ORIGIN}
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_SERVER_URL=${CLIENT_ORIGIN}
    networks:
      - rpg-internal
    depends_on:
      - server
    restart: unless-stopped
